ul
  li('*ngFor'='let item of skill')
    app-skill-group-component('[skillGroup]'='item.skill_group')
    ul
      li('*ngFor'='let skill_item of item.skill_item')
        app-skill-item-component('[skillItem]'='skill_item')
